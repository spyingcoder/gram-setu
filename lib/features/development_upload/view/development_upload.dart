import 'package:flutter/material.dart';

class DevelopmentUploadScreen extends StatelessWidget {
  const DevelopmentUploadScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Development Upload')),
    );
  }
}
